/* Посчитать сумму всех значений из таблицы orders */
SELECT COUNT(*)
FROM orders 

/* Посчитать количество уникальных названий стран колонки country из таблицы employees */
SELECT COUNT(DISTINCT country)
FROM employees